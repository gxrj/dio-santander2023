package estrutural.proxy;
/**
 * Interface implementada pelo proxy e pelo serviço complexidade simplificada 
 * com o objetivo de apenas demonstar a aplicação do pattern estrutural proxy.
 */
public interface ServicoDePagamento {
    void pagar( Double valor );
}
